Description: The install target in the Makefile causes problems
  when building the debian package. This patch simply removes that
  target.
Forwarded: Doesn't make sense upstream
Author: Ruben Undheim <ruben.undheim@gmail.com>
===================================================================
--- qrouter-1.2.29.orig/Makefile.in
+++ qrouter-1.2.29/Makefile.in
@@ -47,7 +47,7 @@ BININSTALL = ${prefix}/bin
 
 all: $(ALL_TARGET)
 
-install: $(INSTALL_TARGET)
+#install: $(INSTALL_TARGET)
 
 nointerp: qrouter$(EXEEXT)
 
